# Secrets — never commit real credentials
.env

# Internal planning brief + screenshot staging — local only, not for the public repo
glpi-project-plan.md
_screenshot-inbox/

# Docker volume data (if ever bind-mounted locally)
db_data/
glpi_data/
volumes/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor cruft
.vscode/
.idea/
*.swp
*~
