# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# --- GLPI database connection (read by the glpi container) ---
GLPI_DB_HOST=mariadb
GLPI_DB_PORT=3306
GLPI_DB_NAME=glpi
GLPI_DB_USER=glpi
GLPI_DB_PASSWORD=change_me_strong_password
